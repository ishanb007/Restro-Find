// src/pages/FilterSearch.js

import React from 'react';

const FilterSearch = () => {
  return (
    <div>
      <h1>Filter Search</h1>
      <p>Search for restaurants by name, range, and more.</p>
      {/* Add form for filter search (e.g., name, range) */}
    </div>
  );
};

export default FilterSearch;
